import styled from "@emotion/styled";
import { StaticImageData } from "next/image";

interface Props {
  title: string;
  Ratio: number;
  img: StaticImageData;
}

const SkillBox = () => {
  return <></>;
};

export default SkillBox;
